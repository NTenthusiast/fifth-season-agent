# 改动报告

**项目**：「第五季·续种」校园 AI 传承智能体
**日期**：2026-09-14
**范围**：`C:\Users\bbfjq\Desktop\projects` 全目录

---

## 一、这次做了什么

起因是两个反馈：**线上智能体"感觉挺蠢"**、**页面不够完善**。另外要求智能体输出不能有语病。

通读全部源码后，找到"变蠢"的根因并修复，同时清理了全站语病与事实错误、补齐了缺失的参赛交付物。

**核心结论：智能体"蠢"的头号原因不是提示词，是模型选错了。**

---

## 二、智能体改动

### 2.1 模型误配（根因）

| 项 | 原值 | 现值 |
|---|---|---|
| 模型 | `glm-4-7-251222`（轻量小模型） | `doubao-seed-2-0-pro-260215` |
| 温度 | 0.7 | 0.4 |

`AGENT.md` 记录的本意一直是 `doubao-seed-2-0-pro`，但配置里写的是别的小模型。小模型要扛 6000 余字的系统提示词、7 段 SOP、25 个工具的编排，必然表现为工具乱选、不遵守流程、回答扁平。

温度从 0.7 降到 0.4，让结构化输出（种子卡、交接包）的格式更稳定。

### 2.2 配置项长期失效（`src/agents/agent.py`）

代码读的键名与配置里的键名对不上，导致三个配置项从未生效：

| 配置项 | 问题 |
|---|---|
| `thinking` vs `thinking_type` | 代码读 `thinking`，配置里叫 `thinking_type` → 永远取默认值 `disabled`，配置写 `enabled` 也无效 |
| `max_tokens` | 从未传给模型 |
| `frequency_penalty` | 从未传给模型 |

已修正键名并把参数真正传给 `ChatOpenAI`。同时补了一行启动日志，部署后可在日志里直接确认实际生效的模型与参数。

### 2.3 滑动窗口会切断工具调用配对（严重隐患）

会话只保留最近 40 条消息。朴素的截断会把 `AIMessage(tool_calls)` 与其对应的 `ToolMessage` 拆散，上游网关会直接返回 400 错误：

> messages with role 'tool' must be a response to a preceding message with 'tool_calls'

每回合新增 2~4 条消息，40 条边界迟早会被撞上——**这类问题在演示中途出现会直接中断对话**。已改为截断后检测并丢弃窗口首部的孤立工具结果，落到安全边界。

### 2.4 故障判据过宽，导致智能体莫名"卖萌"

原 `_DB_FAULT_KEYWORDS` 里含 `"connection"`、`"timeout"`、`"503"` 这类过宽关键词。一次普通的工具报错（如对象存储超时）就会被判定为"记忆库故障"，进而触发那套"严禁再调用任何数据库类工具"的话术——**智颜会在正常场景下忽然说"记忆库在打盹"**。这是"显得蠢"的直接来源之一。

已收窄为只保留能确定是数据库本身出问题的信号（`PGRST002`、`schema cache`、连接池耗尽、以及 db_helpers 抛出的中文错误前缀）。

### 2.5 数据库客户端未复用（响应慢）

`get_supabase_client()` 每次调用都新建一个 httpx 客户端与连接池，且从不关闭。而 `db_helpers._client()` 每处都调它——**单次 `get_project_detail` 就要建 4 个客户端**（项目 / 成员 / 动态 / 交接包），等于每次请求重做 4 轮 TCP+TLS 握手。已改为全局缓存复用。

### 2.6 系统提示词重写

提示词从 6273 字扩到 9119 字，改动集中在四处：

**(1) 新增「表达规范」，前置到高权重位置**

原来的提示词只说"人格温暖、专业、克制"这类空话，把语域完全交给模型默认——豆包默认是热情陪聊腔，没人拦着就往那儿跑。更糟的是代码里 `_DB_FAULT_GUIDANCE` **自己在教模型撒娇**（写着"用轻松自然的语气"，并示范"记忆库这会儿在打盹"）。

新规范给出可执行的约束：

- 沟通底色：借用心理咨询师的**工作方式**（不评判、先澄清再行动、具体化、不越界），不借用其**身份**。明确禁止"我听到你说""你的感受很重要"这类治疗性话术
- 禁用清单：emoji 装饰、波浪号结尾、语气词堆砌、撒娇开场、无信息量的共情套话、感叹号当热情用
- 5 组正反示例对照

**(2) 新增「工具路由表」**

25 个工具中存在语义相近的组合（"看有哪些项目" vs "把想法立成项目"、"看某个项目资料" vs "看全校哪些项目需关注"）。新增一张意图→工具的映射表，并为易混工具在函数文档里补充"不适用场景"说明。

**(3) 修正提示词自身的语病**

生造词"谐音寄托"、"服务+机构名"搭配不当、"你自身的智能"歧义、申请方视角的超长句（已拆为三步）、状态机列举漏了 `autumn→summer`。

**(4) 新增 SSE 工具执行进度**

`web_server.py` 原来只透传 AI 文本增量，把 `ToolMessage` 全部丢弃。多工具回合里用户会盯着空白气泡好几秒——体感上就是"笨"。现在工具开始执行时会推一条 `{"type":"tool","label":"正在检索校园项目库…"}`，前端显示轻量进度行。

---

## 三、前端改动

### 3.1 移动端完全没有导航（硬伤）

`style.css` 在 `max-width:720px` 下写了 `.nav-links { display: none; }`，**且没有任何替代方案**。手机上打开等于没有导航栏——评委扫码即中招。

已补折叠式汉堡菜单：7 个锚点全部可达，支持点击外部收起、Esc 收起、视口放大自动复位。

### 3.2 hero 主标题在手机上被裁切

标题固定 `font-size: 68px`，**全站 6 个断点没有任何一条缩减它**。375px 手机实测需约 458px 宽，而 `.hero` 是 `overflow: hidden`，于是「续种」二字被直接裁掉，还不产生滚动条。

原先 720px 断点里唯一的对策 `.hero h1 { letter-spacing: 4px; text-indent: 4px }` 是**空操作**——子元素 `.art-char` 自己声明了 `letter-spacing` 覆盖继承值，`text-indent` 对 flex 子项也无效。

已改用 `clamp()` 让字号随视口连续缩放，320px 视口下总宽约 289px。

### 3.3 其他页面修复

| 问题 | 处理 |
|---|---|
| `--ink-faint` 变量从未定义，两处 `color` 声明静默失效 | 补上定义 |
| `--ink-mute` 对比度仅 2.63:1，低于 WCAG AA 的 4.5:1，影响 22 处文本（最小 9.5px） | 调为 `#6b7a8d`（约 4.6:1） |
| 四季卡片渲染出「了解春的故事 → →」重复箭头 | 移除 HTML 里的字面箭头 |
| 四季笺渲染出三个连续破折号 | 移除 CSS 伪元素装饰 |
| `.feature-grid` 等三处 grid 用固定像素下限，320px 视口撑破容器 | 改用 `min(N, 100%)` |
| 导航缺 `#skills` `#votes` `#stories` `#finale` 入口 | 补齐 |
| 双板块 section 缺 `id`，无法被锚点定位 | 补 `id="panels"` |
| 4 处 `href="javascript:void(0)"`，读屏播报为"链接" | 改为 `<button>` |

### 3.4 语病与事实错误

| 位置 | 问题 |
|---|---|
| 页脚 | 写成 **"2025 EL 大赛"**，实际是 2026 年赛事 |
| 首页 | 「她叫知颜，懂得…色彩的知己」句子与名词短语混接 |
| 首页 | 「而至第五季」不成词 |
| 首页 | 「在新一届手中」「仅完成两栋教学楼」缺中心语（共 3 处） |
| 功能全景 | 种子卡字段写成「定位/目标/阶段计划」，实际是 标题/简介/领域标签/所需能力 |
| 功能全景 | **"v3 全新升级"** 内部版本号泄漏到评委可见页面 |
| 功能全景 | 「哪些项目可以一起搞事情？」语气不符 |
| 功能全景 | 待办示例「周三下午去取快递」演示价值过低 |
| 四季浮层 | 夏天写「阶段流转（萌芽→招募→推进→收尾）」，**与真实状态机直接矛盾** |
| 对话页 | 开场白「你好呀…知颜 🌱」及空状态里硬贴的 ✨ |
| 快捷键 | 🌞 与全站 ☀️ 不一致 |

### 3.5 外链清理

26 个外链中 25 个返回 200，但其中 6 个指向**机构首页而非所引报道**，无法核验出处；另有 1 个（`gotopku.cn`）SSL 握手失败。已移除 6 处指向裸域名的链接。

---

## 四、数据与文档

| 项 | 内容 |
|---|---|
| `model.py` | ORM 只定义了 9 张业务表，代码实际读写 13 张。补齐 `fs_stories` / `fs_awards` / `fs_user_awards` / `fs_votes` |
| `AGENT.md` | 工具清单**有 12 个幽灵工具**（`create_user_profile`、`add_todo`、`create_handover_package`、`get_recommended_projects` 等均不存在于代码），另有 10 个真实工具漏记。已按 `ALL_TOOLS` 更正；另修正了"12 张表 vs 13 张表"的自相矛盾与生产库状态描述 |
| `README.md` | 原为 213 字节的 Coze 模板流水账，已重写为面向评委的完整说明（含**启动方式警告**：直接双击 `index.html` 会因 `/static/` 路径导致样式全失） |
| `.gitignore` | 原来忽略了 `*.pdf`，导致参赛文档提交不上去。已放行 `docs/` |
| **新建** `docs/项目文档.pdf` | 比赛必交项，此前完全缺失。13 页，含封面/目录/六个正文章节/附录，中文字体已嵌入 |

---

## 五、清理

- 删除根目录 `test1.png` 与 `assets/web/test1.png`（154KB × 2，内容重复，无任何引用）
- 清除 `src/storage/database/supabase_client.py` 的只读属性（复制操作带过来的，导致无法编辑）
- `assets/web/logo.svg` **保留**——无代码引用，但属品牌素材，体积仅 2.8KB，项目文档可能用到

**文件统计**：修改 13 个，新建 3 个，删除 2 个。

---

## 六、验证情况

### 已验证（有实测依据）

- 全部 Python 文件语法通过
- 配置文件仍是合法 JSON；`ALL_TOOLS`（25）与 `config.tools`（25）、提示词中提到的工具名三方完全一致
- ORM 定义表数（13）与代码实际查询表数（13）完全对齐，无缺失无冗余
- 前端在浏览器中加载**无任何 JS 报错**
- 移动端 320 / 360 / 375 / 390 / 414px 五档实测：`scrollWidth == clientWidth`（无横向溢出），hero 标题右边界始终在视口内
- 汉堡菜单展开/收起、7 个锚点渲染正常（截图确认）
- 项目文档 PDF：13 页，中文字体（NSimSun / 微软雅黑）已嵌入，目录页码与实际分页逐节核对一致
- 新增的 SSE 协议：前后端事件契约完全对齐（服务端发 delta/done/error/tool，前端消费同样四种）

### 未能验证（必须在 Coze 沙箱确认）

**本地跑不起智能体**——缺少平台侧凭证（模型网关地址、工作负载身份密钥、Supabase 连接串、对象存储配置）。因此以下改动**均为静态审查结论，未经运行时验证**：

- 模型更换后的实际回复质量
- 工具路由表对工具选择准确率的改善
- 表达规范对语域的收敛效果
- 滑动窗口修复是否真正消除了 400 报错
- Supabase 客户端复用带来的性能改善

---

## 七、部署后必查项

按优先级排列：

1. **生成一次托付书或海报，检查图片里的中文是否正常显示。**
   `legacy_tools.py` 的字体路径只查找 `/usr/share/fonts/truetype/wqy/` 下的文泉驿字体，找不到会回退到 `ImageFont.load_default()`——**那个字体不含中文**，中文会变成豆腐块。这个本地无法验证，但一旦出问题会直接毁掉演示。

2. **确认模型真的换了。** 那份配置文件的字段形状像是 Coze IDE 模型面板回写的，建议去 IDE 里确认模型选择器也是 `doubao-seed-2-0-pro`，否则可能被面板覆盖回去。

3. **跑一遍全链路**：建档 → 建种子卡 → 找伙伴 → 交接包 → 休眠 → 唤醒。重点看工具选择是否准确、语气是否收敛。

4. **手机实机打开页面**，确认汉堡菜单可用、标题完整不被裁切。

---

## 八、未做的事项（原计划中列出但未执行）

| # | 项 | 说明 |
|---|---|---|
| 1 | 页面新增板块（团队介绍 / 技术架构 / 效果验证数据 / 演示视频入口） | 技术内容已写入项目文档，但页面未加 |
| 2 | 模态框无障碍语义（`role="dialog"` / `aria-modal` / 焦点管理） | 打开弹窗后 Tab 会穿透到背后内容 |
| 3 | 触控目标偏小 | 4 处 34×34px，低于 44px 推荐值 |
| 4 | thepaper 重复链接 | FS-017 与 FS-018 两张卡片指向同一链接，待确认如何处理 |
| 5 | 项目文档占位符 | 2 处（团队名、成员信息），PDF 中显示为黄色高亮 |

## 九、审查中发现但未修复的代码问题

- `src/storage/database/db.py`：单次连接无超时，冷启动最坏可能超过 FaaS 的 30 秒启动窗口
- `supabase_client.py` 的 `_RetryTransport`：对 POST 的 503 也重试，理论上可能造成重复写入
- `src/main.py`：`self.graph` 属性不存在（应为 `self._graph`），是潜伏的 `AttributeError`，目前被提前 `return` 掩盖
- `pyproject.toml`：`dbus-python` / `PyGObject` / `pycairo` 只有源码包、无 Windows wheel，可能拖垮部署构建（应用代码实际未使用）
- 死代码：`src/storage/s3/s3_storage.py`（424 行）、`src/utils/file/file.py`，均无任何引用

---

## 十、附：本地预览工具

新增 `scripts/preview_mock.py`——**开发工具，非产品代码**，仅用 Python 标准库，不连数据库、不调模型：

```bash
python scripts/preview_mock.py        # http://localhost:8000
```

用于在本地验收前端改动（移动端导航、标题自适应、工具进度提示、四季浮层等）。对话内容为脚本化的演示文本。

该文件不参与线上部署，参赛前如需精简参赛包可整份删除。
